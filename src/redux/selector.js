export const dataName = state => state.auth.user.name;
export const dataEmail = state => state.auth.user?.email;
export const dataToken = state => state.auth.token;
export const contacts = state => state.contacts.contacts;
export const filters = state => state.filter.filter;
